# 動画リストのフォルダ分け機能 タスクリスト

## 進捗状況

凡例：`[ ]` 未着手 / `[x]` 完了

---

## Phase 1: データモデル更新

- [x] 1-1. `VideoFolder.swift` を新規作成する（`id` / `name` / `order` / `isDefault`）
- [x] 1-2. `VideoItem` に `folderId: UUID?` を追加し、`init(url:order:folderId:)` に変更する

## Phase 2: サービス層追加

- [x] 2-1. `VideoFolderRepository.swift` を新規作成する
- [x] 2-2. フォルダ未保存（初回起動）・空配列の場合に「デフォルト」フォルダを自動生成する処理を実装する

## Phase 3: ViewModel更新

- [x] 3-1. `VideoListViewModel` に `folders` / `selectedFolderId` / `displayedVideoItems` を追加する
- [x] 3-2. `migrateLegacyVideosIfNeeded()` を実装し、`folderId == nil` の既存動画をデフォルトフォルダへ移行・保存する
- [x] 3-3. `isAtLimit(for:)` をフォルダ単位に変更し、`isAddDisabled` を追加する
- [x] 3-4. `addVideo` に `folderId` 引数を追加し、対象フォルダの上限チェック・エラーメッセージを実装する
- [x] 3-5. `deleteVideo` / `moveVideo` を `displayedVideoItems` 基準に変更する（`moveVideo` はフィルタ後の並び替え結果を元配列にマージする）
- [x] 3-6. `addFolder` / `renameFolder` / `deleteFolder` とフォルダ名バリデーション（空文字不可・20文字以内）を実装する
- [x] 3-7. `deleteFolder` は `isDefault` のフォルダに対して何もしない、削除対象が選択中フォルダの場合は「すべて」タブに戻すことを実装する

## Phase 4: View更新

- [x] 4-1. `FolderTabBarView.swift` を新規作成する（「すべて」固定タブ・フォルダタブ・「＋」追加ボタン）
- [x] 4-2. フォルダタブの長押しコンテキストメニュー（名前を変更・削除）を実装する（`isDefault` は削除メニューなし）
- [x] 4-3. フォルダ削除時の確認アラートを実装する
- [x] 4-4. フォルダ追加・名前変更用のアラート（`TextField` 入力）を実装する
- [x] 4-5. `VideoListView` に `FolderTabBarView` を組み込み、表示リストを `displayedVideoItems` に変更する
- [x] 4-6. `VideoListView` の FAB 無効化・上限メッセージ表示を `isAddDisabled` ベースに変更する
- [x] 4-7. `AddVideoView` に追加先フォルダを選択する `Picker` を追加し、初期選択値（選択中フォルダ／デフォルトフォルダ）を実装する

## Phase 5: 動作確認

- [X] 5-1. アプリ初回起動時に「デフォルト」フォルダが自動生成され、「すべて」「デフォルト」タブが表示されることを確認する
- [X] 5-2. フォルダ導入前の既存動画データで起動し、全動画が「デフォルト」フォルダに表示されることを確認する（10件超のデータでも削除されないこと）
- [X] 5-3. フォルダを新規作成できることを確認する
- [X] 5-4. フォルダ名を変更できることを確認する
- [X] 5-5. フォルダを削除すると、フォルダ内の動画も削除され、「すべて」タブに切り替わることを確認する
- [X] 5-6. 「デフォルト」フォルダに削除メニューが表示されないことを確認する
- [X] 5-7. 動画追加時にフォルダを選択して保存できることを確認する
- [X] 5-8. フォルダごとに10件登録すると、そのフォルダでのみ＋ボタンが無効化され上限メッセージが表示されることを確認する（他フォルダ・「すべて」タブでは無効化されない）
- [X] 5-9. 「すべて」タブで全フォルダの動画がまとめて表示されることを確認する
- [X] 5-10. フォルダタブを切り替えて動画一覧がフィルタされることを確認する
- [X] 5-11. 特定フォルダタブ内で動画を並び替えても、他フォルダの動画の並びに影響しないことを確認する
- [X] 5-12. アプリ再起動後もフォルダ構成・動画のフォルダ割り当てが保持されることを確認する

## Phase 6: テスト追加

- [x] 6-1. `VideoFolderRepositoryTests` を新規作成する（保存・読込・初回デフォルト生成）
- [x] 6-2. `VideoRepositoryTests` に `folderId` なし旧データの読込テストを追加する
- [x] 6-3. `VideoListViewModel` の移行処理・フォルダ単位上限チェック・フォルダCRUDのテストを追加する

## 完了条件

`requirements.md` の受け入れ条件がすべてチェックされた状態。
